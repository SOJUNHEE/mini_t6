기존에 쓰던 api-vault 폴더를 이 위치에 통째로 덮어쓰세요.

  mini_t6_test/api-vault/
  ├─ API_KEYS_FORM.txt        ← 키 입력표 (커밋 금지)
  ├─ credentials/.env         ← 자동 생성됨 (커밋 금지)
  ├─ registry/api_registry.csv
  ├─ docs/발급_남은목록.txt
  ├─ scripts/api_keys.py      ← 코드에서 이것만 import 한다
  ├─ specs/
  └─ reference_data/
      ├─ customs/   관세청 HS부호·관세율표 (43개국)
      └─ ksure/     무역보험공사 국가신용등급

.gitignore가 credentials/ 와 reference_data/ 를 이미 제외하고 있습니다.
덮어쓴 뒤 이 안내 파일은 지우세요.

키 사용법:
    import sys; sys.path.insert(0, "api-vault/scripts")
    from api_keys import get_key
    key = get_key("DATA_GO_KR_API_KEY")   # 없으면 None → Provider가 대체 경로로

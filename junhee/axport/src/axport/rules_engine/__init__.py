"""판정 규칙 계층 (명세서 §6, §11). — 미구현

책임
    3층 판정 구조를 산출한다 (rules/README.md §2).
    필수 조건 검토 / 사업적 적합도 / 근거 충족 상태.
    종합 판정은 게이트 G1~G5 로만 도출한다 (rules/README.md §3).

금지
    사업성 점수로 필수 조건 미충족을 상쇄하지 않는다.
    모델이 점수 기준·규정 조건을 스스로 만들어 확정하지 않는다.
    미검토 규칙을 운영 판정에 사용하지 않는다.

현재 상태
    rule_set_usable_in_production = false
    별표2의2·별표6 미확보, 별표1~4 원문대조 필요.
    따라서 규제 영역은 withheld 로 고정된다.
"""

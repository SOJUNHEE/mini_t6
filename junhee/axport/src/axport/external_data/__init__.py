"""외부 데이터 연결 계층 (명세서 §7, §11). — 미구현

책임
    제공기관별 연결 모듈을 분리하고 출력 구조를 표준화한다.
    출처 URL·기준일·수집일·적용 기간·해시·버전을 저장한다.
    연결 실패 / 자료 없음 / 권한 없음 / 오래된 저장 자료 사용을
    구분해 반환한다.

금지
    장애 시 샘플 자료로 자동 대체해 실제 결과처럼 표시하지 않는다.
    미국 관세표를 다른 국가 관세 자료로 사용하지 않는다.
    verified 가 '검증완료' 아닌 소스를 확정 판정 근거로 쓰지 않는다.

키 취급
    api-vault/scripts/api_keys.py 의 get_key / require_key 로만 읽는다.
    키 값을 화면·로그에 출력하지 않는다.
    대장: ../api-vault/registry/api_registry.csv
    검증 스크립트: ../api-vault/scripts/verify_apis.py
"""

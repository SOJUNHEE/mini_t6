"""지표 계산 계층 (명세서 §11). — 미구현

책임
    영역별(시장성·가격·물류·안정성) 지표를 계산한다.
    모든 지표는 MeasuredValue 로 반환한다 (rules/README.md §1).
    금액 계산과 화면 반올림을 분리한다.

금지
    결측을 0으로 치환하지 않는다. 실제 값 0 / 미입력 / 미평가 /
    해당 없음 / 조회 실패를 같은 값으로 표현하지 않는다.
    부분 합산으로 낙관적 값을 만들지 않는다.
"""
